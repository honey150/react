import {PostList} from '../store/post-list-store.jsx'
import {useEffect, useContext} from 'react'
import '../routes/App.css'

const WelcomeMessage = () =>{ 

  return (
  <h1>There are currently no post available.</h1>
);
}
export default WelcomeMessage;